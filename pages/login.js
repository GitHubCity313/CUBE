import React from "react";
import PropTypes from "prop-types";
import { Grid, Box } from "@mui/material";
import Layout from "../components/Layout";

const Login = () => {
  return (
    <Layout title="Cube | Login">
      <Box> Ici le beau login </Box>
    </Layout>
  );
};

export default Login;
